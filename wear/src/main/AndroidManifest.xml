<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".tile.TileConfigActivity"
            android:exported="true"
            android:label="@string/tile_action_label" />

        <receiver
            android:name=".tile.TileActionReceiver"
            android:exported="false" />

        <service
            android:name=".tile.TileGridDataListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/apollo-deck/tile-grid" />
            </intent-filter>
        </service>

        <service
            android:name=".tile.ActionTileService"
            android:exported="true"
            android:label="@string/tile_action_label"
            android:description="@string/tile_action_description"
            android:icon="@android:drawable/sym_def_app_icon"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_action_preview" />
        </service>

        <service
            android:name=".tile.MultiActionTileService"
            android:exported="true"
            android:label="@string/tile_multi_action_label"
            android:description="@string/tile_multi_action_description"
            android:icon="@android:drawable/sym_def_app_icon"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_multi_action_preview" />
        </service>

    </application>

</manifest>
