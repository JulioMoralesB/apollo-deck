<?xml version="1.0" encoding="utf-8"?>
<!-- Plain RemoteViews layout for the home-screen action widget. Built and
     pushed directly by WidgetRemoteViews.kt rather than through Jetpack
     Glance's Composable/session pipeline — see that file for why. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="8dp">

        <!-- wrap_content, not a fixed size: the pushed bitmap is already
             rendered at the chosen icon size in pixels (see
             WidgetRemoteViews.kt), so the ImageView just takes its natural
             size — the only way to vary displayed size across API levels,
             since RemoteViews.setViewLayoutWidth/Height needs API 31+. -->
        <ImageView
            android:id="@+id/widget_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/widget_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:gravity="center"
            android:textSize="11sp"
            android:fontFamily="monospace" />

    </LinearLayout>
</FrameLayout>
